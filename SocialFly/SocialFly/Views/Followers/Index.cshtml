@model IEnumerable<SocialFly.Follower>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FollowerCount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FollowerCount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FollowerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FollowerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FollowerId })
        </td>
    </tr>
}

</table>
