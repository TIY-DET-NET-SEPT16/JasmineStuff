@model IEnumerable<SocialFly.Compensation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CompPay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CompPay)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CompId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CompId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CompId })
        </td>
    </tr>
}

</table>
